---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<Layout>
	<div class="page-container">
		<Header />
		<main>
			<!-- Hero Section -->
			<section class="contact-hero">
				<div class="hero-content">
					<div class="hero-text fade-in">
						<div class="hero-badge">
							<span>CONTACT</span>
						</div>
						<h1 class="hero-title">
							Laten we <span class="highlight">samenwerken</span> aan uw veiligheid
						</h1>
						<p class="hero-description">
							Neem vandaag nog contact op voor een vrijblijvend adviesgesprek en ontdek hoe wij uw bedrijf of woning kunnen beveiligen.
						</p>
						<div class="hero-cta">
							<a href="#contact-form" class="cta-primary">Direct Contact</a>
							<a href="tel:+31201234567" class="cta-secondary">
								<svg width="20" height="20" viewBox="0 0 24 24" fill="none">
									<path d="M22 16.92V19.92C22 20.52 21.39 21 20.67 21C9.71 21 1 12.79 1 2.68C1 2.3 1.37 2 1.78 2H4.89C5.45 2 5.89 2.39 5.89 2.85C5.89 5.08 7.89 7 10 7H14C14.55 7 15 7.45 15 8V12C15 12.55 15.45 13 16 13H20C20.55 13 21 13.45 21 14V16C21 16.55 21.45 17 22 17C22.55 17 23 16.55 23 16C23 16.55 22.55 17 22 17C21.45 17 21 16.55 21 16V14H16V8C16 6.9 15.1 6 14 6H10C7.24 6 5 3.76 5 1C5 0.45 4.55 0 4 0H1C0.45 0 0 0.45 0 1C0 12.6 10.4 23 22 23C23.1 23 24 22.1 24 21V18C24 17.45 23.55 17 23 17C22.45 17 22 17.45 22 18V16.92Z" fill="currentColor"/>
								</svg>
								+31 20 123 4567
							</a>
						</div>
					</div>
					<div class="hero-visual slide-in-right">
						<div class="floating-cards">
							<div class="card card-1">
								<div class="card-icon">
									<svg width="24" height="24" viewBox="0 0 24 24" fill="none">
										<path d="M12 2L2 7V12C2 16.5 4.23 20.68 7.62 23.15L12 24L16.38 23.15C19.77 20.68 22 16.5 22 12V7L12 2Z" fill="#25c1ce"/>
									</svg>
								</div>
								<span>24/7 Monitoring</span>
							</div>
							<div class="card card-2">
								<div class="card-icon">
									<svg width="24" height="24" viewBox="0 0 24 24" fill="none">
										<circle cx="12" cy="12" r="10" stroke="#25c1ce" stroke-width="2"/>
										<path d="M12 6V12L16 14" stroke="#25c1ce" stroke-width="2" stroke-linecap="round"/>
									</svg>
								</div>
								<span>Snelle Respons</span>
							</div>
							<div class="card card-3">
								<div class="card-icon">
									<svg width="24" height="24" viewBox="0 0 24 24" fill="none">
										<path d="M9 12L11 14L15 10M21 12C21 16.9706 16.9706 21 12 21C7.02944 21 3 16.9706 3 12C3 7.02944 7.02944 3 12 3C16.9706 3 21 7.02944 21 12Z" stroke="#25c1ce" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
									</svg>
								</div>
								<span>Betrouwbaar</span>
							</div>
						</div>
					</div>
				</div>
			</section>

			<!-- Contact Methods Grid -->
			<section class="contact-methods">
				<div class="methods-container">
					<div class="methods-header fade-in">
						<h2 class="methods-title">Kies uw <span class="highlight">contactmethode</span></h2>
						<p class="methods-description">Wij staan klaar om u te helpen via het kanaal dat u het beste uitkomt</p>
					</div>

					<div class="methods-grid">
						<div class="method-card stagger-item">
							<div class="method-icon">
								<svg width="40" height="40" viewBox="0 0 24 24" fill="none">
									<path d="M22 16.92V19.92C22 20.52 21.39 21 20.67 21C9.71 21 1 12.79 1 2.68C1 2.3 1.37 2 1.78 2H4.89C5.45 2 5.89 2.39 5.89 2.85C5.89 5.08 7.89 7 10 7H14C14.55 7 15 7.45 15 8V12C15 12.55 15.45 13 16 13H20C20.55 13 21 13.45 21 14V16C21 16.55 21.45 17 22 17C22.55 17 23 16.55 23 16C23 16.55 22.55 17 22 17C21.45 17 21 16.55 21 16V14H16V8C16 6.9 15.1 6 14 6H10C7.24 6 5 3.76 5 1C5 0.45 4.55 0 4 0H1C0.45 0 0 0.45 0 1C0 12.6 10.4 23 22 23C23.1 23 24 22.1 24 21V18C24 17.45 23.55 17 23 17C22.45 17 22 17.45 22 18V16.92Z" fill="#25c1ce"/>
								</svg>
							</div>
							<h3>Telefoon</h3>
							<p class="method-contact"><a href="tel:+31201234567">+31 20 123 4567</a></p>
							<span class="method-time">Ma-Vr: 9:00-18:00</span>
							<div class="method-action">
								<a href="tel:+31201234567" class="btn-outline">Nu Bellen</a>
							</div>
						</div>

						<div class="method-card stagger-item">
							<div class="method-icon">
								<svg width="40" height="40" viewBox="0 0 24 24" fill="none">
									<path d="M3 8L12 13L21 8V12L12 17L3 12V8Z" fill="#25c1ce"/>
									<path d="M3 5L12 10L21 5V19C21 20.1 20.1 21 19 21H5C3.9 21 3 20.1 3 19V5Z" stroke="#25c1ce" stroke-width="2"/>
								</svg>
							</div>
							<h3>E-mail</h3>
							<p class="method-contact"><a href="mailto:info@secudat.nl">info@secudat.nl</a></p>
							<span class="method-time">Reactie binnen 24 uur</span>
							<div class="method-action">
								<a href="mailto:info@secudat.nl" class="btn-outline">E-mail Sturen</a>
							</div>
						</div>

						<div class="method-card stagger-item">
							<div class="method-icon">
								<svg width="40" height="40" viewBox="0 0 24 24" fill="none">
									<path d="M21 15C21 15.5304 20.7893 16.0391 20.4142 16.4142C20.0391 16.7893 19.5304 17 19 17H7L3 21V5C3 4.46957 3.21071 3.96086 3.58579 3.58579C3.96086 3.21071 4.46957 3 5 3H19C19.5304 3 20.0391 3.21071 20.4142 3.58579C20.7893 3.96086 21 4.46957 21 5V15Z" stroke="#25c1ce" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
								</svg>
							</div>
							<h3>WhatsApp</h3>
							<p class="method-contact"><a href="https://wa.me/31612345678">+31 6 123 45678</a></p>
							<span class="method-time">Direct beschikbaar</span>
							<div class="method-action">
								<a href="https://wa.me/31612345678" class="btn-outline">WhatsApp</a>
							</div>
						</div>
					</div>

					<!-- Emergency Card - Full Width Below -->
					<div class="emergency-section stagger-item">
						<div class="emergency-card-large">
							<div class="emergency-content">
								<div class="emergency-icon">
									<svg width="50" height="50" viewBox="0 0 24 24" fill="none">
										<path d="M13 2L3 14H12L11 22L21 10H12L13 2Z" fill="white"/>
									</svg>
								</div>
								<div class="emergency-details">
									<h3>Spoed? Direct Contact Nodig</h3>
									<p>24/7 bereikbaar voor noodsituaties en dringende hulp</p>
									<div class="emergency-contact">
										<a href="tel:+31612345678" class="emergency-phone">+31 6 123 45678</a>
									</div>
								</div>
							</div>
							<div class="emergency-action">
								<a href="tel:+31612345678" class="btn-emergency-large">
									<svg width="24" height="24" viewBox="0 0 24 24" fill="none">
										<path d="M13 2L3 14H12L11 22L21 10H12L13 2Z" fill="white"/>
									</svg>
									<span>Direct Bellen</span>
								</a>
							</div>
						</div>
					</div>
				</div>
			</section>

			<!-- Contact Form Section -->
			<section id="contact-form" class="contact-form-section">
				<div class="form-container">
					<div class="form-content">
						<div class="form-info slide-in-left">
							<h2>Stuur ons een <span class="highlight">bericht</span></h2>
							<p>Vertel ons over uw beveiligingswensen en ontvang een vrijblijvend advies op maat.</p>

							<div class="form-features">
								<div class="feature-item">
									<div class="feature-icon">
										<svg width="20" height="20" viewBox="0 0 24 24" fill="none">
											<path d="M5 13L9 17L19 7" stroke="#25c1ce" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
										</svg>
									</div>
									<span>Vrijblijvend advies</span>
								</div>
								<div class="feature-item">
									<div class="feature-icon">
										<svg width="20" height="20" viewBox="0 0 24 24" fill="none">
											<path d="M5 13L9 17L19 7" stroke="#25c1ce" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
										</svg>
									</div>
									<span>Reactie binnen 24 uur</span>
								</div>
								<div class="feature-item">
									<div class="feature-icon">
										<svg width="20" height="20" viewBox="0 0 24 24" fill="none">
											<path d="M5 13L9 17L19 7" stroke="#25c1ce" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
										</svg>
									</div>
									<span>Maatwerk oplossingen</span>
								</div>
							</div>

							<div class="info-card">
								<h3>Heeft u dringende hulp nodig?</h3>
								<p>Voor directe assistentie bij noodsituaties</p>
								<a href="tel:+31612345678" class="emergency-btn">
									<svg width="20" height="20" viewBox="0 0 24 24" fill="none">
										<path d="M13 2L3 14H12L11 22L21 10H12L13 2Z" fill="white"/>
									</svg>
									Spoednummer: +31 6 123 45678
								</a>
							</div>
						</div>

						<div class="form-wrapper slide-in-right">
							<form id="contactForm" class="contact-form">
								<div class="form-row">
									<div class="form-group">
										<label for="firstName">Voornaam *</label>
										<input type="text" id="firstName" name="firstName" required>
										<span class="form-error"></span>
									</div>
									<div class="form-group">
										<label for="lastName">Achternaam *</label>
										<input type="text" id="lastName" name="lastName" required>
										<span class="form-error"></span>
									</div>
								</div>

								<div class="form-group">
									<label for="contact-email">E-mailadres *</label>
									<input type="email" id="contact-email" name="email" required>
									<span class="form-error"></span>
								</div>

								<div class="form-group">
									<label for="contact-phone">Telefoonnummer</label>
									<input type="tel" id="contact-phone" name="phone">
									<span class="form-error"></span>
								</div>

								<div class="form-group">
									<label for="company">Bedrijfsnaam</label>
									<input type="text" id="company" name="company">
									<span class="form-error"></span>
								</div>

								<div class="form-group">
									<label for="subject">Onderwerp *</label>
									<select id="subject" name="subject" required>
										<option value="">Selecteer een onderwerp</option>
										<option value="advies">Vrijblijvend advies</option>
										<option value="offerte">Offerte aanvragen</option>
										<option value="installatie">Installatie</option>
										<option value="onderhoud">Onderhoud</option>
										<option value="support">Technische support</option>
										<option value="spoed">Spoedgeval</option>
										<option value="anders">Anders</option>
									</select>
									<span class="form-error"></span>
								</div>

								<div class="form-group">
									<label for="contact-message">Bericht *</label>
									<textarea id="contact-message" name="message" rows="5" required placeholder="Beschrijf uw vraag of wens..."></textarea>
									<span class="form-error"></span>
								</div>

								<div class="form-checkbox">
									<input type="checkbox" id="privacy" name="privacy" required>
									<label for="privacy">
										Ik ga akkoord met de <a href="/privacy">privacyvoorwaarden</a> en geef toestemming om contact met mij op te nemen.
									</label>
									<span class="form-error"></span>
								</div>

								<div class="form-actions">
									<button type="submit" class="submit-btn">
										<span>Verstuur Bericht</span>
										<svg width="20" height="20" viewBox="0 0 24 24" fill="none">
											<path d="M5 12H19M19 12L12 5M19 12L12 19" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
										</svg>
									</button>
								</div>

								<div id="formMessage" class="form-message"></div>
							</form>
						</div>
					</div>
				</div>
			</section>

			<!-- Map Section -->
			<section class="map-section">
				<div class="map-container">
					<div class="map-info fade-in">
						<h2>Bezoek ons <span class="highlight">kantoor</span></h2>
						<p>Wij ontvangen u graag op afspraak voor een persoonlijk gesprek.</p>

						<div class="address-card">
							<div class="address-header">
								<div class="address-icon">
									<svg width="30" height="30" viewBox="0 0 24 24" fill="none">
										<path d="M21 10C21 17 12 23 12 23C12 23 3 17 3 10C3 5.58172 6.58172 2 11 2C12.4305 2 13.7714 2.40531 15 3.08334C16.2286 2.40531 17.5695 2 19 2C21.2091 2 23 3.79086 23 6V10H21Z" stroke="#25c1ce" stroke-width="2"/>
										<path d="M12 12V12.01" stroke="#25c1ce" stroke-width="2" stroke-linecap="round"/>
									</svg>
								</div>
								<h3>Secudat Hoofdkantoor</h3>
							</div>
							<div class="address-details">
								<p class="address">Security Plaza 123<br/>1234 AB Beveiligingstad</p>
								<p class="directions">
									<a href="#" class="directions-link">Routebeschrijving →</a>
								</p>
							</div>
						</div>

						<div class="office-hours">
							<h4>Kantooruren</h4>
							<div class="hours-list">
								<div class="hours-item">
									<span>Maandag - Vrijdag</span>
									<span>9:00 - 18:00</span>
								</div>
								<div class="hours-item">
									<span>Zaterdag</span>
									<span>10:00 - 14:00</span>
								</div>
								<div class="hours-item">
									<span>Zondag</span>
									<span>Gesloten</span>
								</div>
							</div>
						</div>
					</div>
					<div class="map-container-interactive fade-in">
						<iframe
							src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d2436.8234257449655!2d4.895168315504463!3d52.37021597978143!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x47c609c1e9b5b5b5%3A0x5b5b5b5b5b5b5b5b!2sAmsterdam%2C%20Netherlands!5e0!3m2!1sen!2snl!4v1234567890"
							width="100%"
							height="400"
							style="border:0; border-radius: 20px;"
							allowfullscreen=""
							loading="lazy"
							referrerpolicy="no-referrer-when-downgrade">
						</iframe>
						<div class="map-overlay">
							<div class="overlay-content">
								<div class="marker-icon">
									<svg width="40" height="40" viewBox="0 0 24 24" fill="none">
										<path d="M21 10C21 17 12 23 12 23C12 23 3 17 3 10C3 5.58172 6.58172 2 11 2C12.4305 2 13.7714 2.40531 15 3.08334C16.2286 2.40531 17.5695 2 19 2C21.2091 2 23 3.79086 23 6V10H21Z" fill="#25c1ce"/>
										<circle cx="12" cy="10" r="2" fill="white"/>
									</svg>
								</div>
								<h3>Secudat Hoofdkantoor</h3>
								<p>Security Plaza 123<br/>1234 AB Beveiligingstad</p>
								<a href="https://www.google.com/maps/dir/?api=1&destination=Security+Plaza+123+1234+AB+Beveiligingstad" target="_blank" class="directions-btn">
									<svg width="20" height="20" viewBox="0 0 24 24" fill="none">
										<path d="M5 12H19M19 12L12 5M19 12L12 19" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
									</svg>
									Routebeschrijving
								</a>
							</div>
						</div>
					</div>
				</div>
			</section>
		</main>
		<Footer />
	</div>
</Layout>

<style>
	.page-container {
		min-height: 100vh;
		overflow-x: hidden;
		width: 100%;
	}

	main {
		padding-top: 100px;
		width: 100%;
	}

	/* Hero Section */
	.contact-hero {
		padding: 80px 20px;
		background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
		position: relative;
		overflow: hidden;
	}

	.contact-hero::before {
		content: '';
		position: absolute;
		top: -50%;
		right: -10%;
		width: 600px;
		height: 600px;
		background: radial-gradient(circle, rgba(37, 193, 206, 0.1) 0%, transparent 70%);
		border-radius: 50%;
		animation: float 6s ease-in-out infinite;
	}

	.contact-hero::after {
		content: '';
		position: absolute;
		bottom: -30%;
		left: -5%;
		width: 400px;
		height: 400px;
		background: radial-gradient(circle, rgba(37, 193, 206, 0.05) 0%, transparent 70%);
		border-radius: 50%;
		animation: float 8s ease-in-out infinite reverse;
	}

	.hero-content {
		max-width: 1200px;
		margin: 0 auto;
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 60px;
		align-items: center;
		position: relative;
		z-index: 1;
	}

	.hero-text {
		padding-right: 40px;
	}

	.hero-badge {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		background: rgba(37, 193, 206, 0.1);
		border: 1px solid rgba(37, 193, 206, 0.2);
		border-radius: 50px;
		padding: 8px 20px;
		margin-bottom: 24px;
	}

	.hero-badge span {
		font-size: 14px;
		font-weight: 600;
		color: #25c1ce;
		letter-spacing: 1px;
	}

	.hero-title {
		font-size: clamp(2.5rem, 5vw, 3.5rem);
		font-weight: 800;
		color: #2c3e50;
		margin-bottom: 24px;
		line-height: 1.1;
	}

	.hero-description {
		font-size: 18px;
		line-height: 1.6;
		color: #64748b;
		margin-bottom: 40px;
	}

	.hero-cta {
		display: flex;
		gap: 20px;
		flex-wrap: wrap;
	}

	.cta-primary {
		display: inline-flex;
		align-items: center;
		padding: 16px 32px;
		background: linear-gradient(135deg, #25c1ce 0%, #20b3bc 100%);
		color: white;
		text-decoration: none;
		border-radius: 12px;
		font-weight: 700;
		font-size: 16px;
		transition: all 0.3s ease;
		box-shadow: 0 10px 30px rgba(37, 193, 206, 0.3);
	}

	.cta-primary:hover {
		transform: translateY(-2px);
		box-shadow: 0 15px 40px rgba(37, 193, 206, 0.4);
	}

	.cta-secondary {
		display: inline-flex;
		align-items: center;
		gap: 12px;
		padding: 16px 32px;
		background: white;
		color: #25c1ce;
		text-decoration: none;
		border-radius: 12px;
		font-weight: 600;
		font-size: 16px;
		transition: all 0.3s ease;
		border: 2px solid rgba(37, 193, 206, 0.1);
	}

	.cta-secondary:hover {
		background: rgba(37, 193, 206, 0.1);
		transform: translateY(-2px);
	}

	/* Floating Cards */
	.hero-visual {
		position: relative;
		height: 400px;
	}

	.floating-cards {
		position: relative;
		height: 100%;
	}

	.card {
		position: absolute;
		background: white;
		padding: 24px;
		border-radius: 16px;
		box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
		border: 1px solid rgba(37, 193, 206, 0.1);
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 12px;
		min-width: 140px;
	}

	.card-icon {
		width: 50px;
		height: 50px;
		background: rgba(37, 193, 206, 0.1);
		border-radius: 12px;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.card span {
		font-weight: 600;
		color: #2c3e50;
		text-align: center;
		font-size: 14px;
	}

	.card-1 {
		top: 20px;
		right: 20px;
		animation: float 4s ease-in-out infinite;
	}

	.card-2 {
		bottom: 80px;
		left: 40px;
		animation: float 5s ease-in-out infinite 1s;
	}

	.card-3 {
		top: 50%;
		right: 120px;
		animation: float 6s ease-in-out infinite 0.5s;
	}

	/* Contact Methods Section */
	.contact-methods {
		padding: 100px 20px;
		background: white;
	}

	.methods-container {
		max-width: 1200px;
		margin: 0 auto;
	}

	.methods-header {
		text-align: center;
		margin-bottom: 60px;
	}

	.methods-title {
		font-size: clamp(2rem, 4vw, 2.5rem);
		font-weight: 800;
		color: #2c3e50;
		margin-bottom: 16px;
	}

	.methods-description {
		font-size: 18px;
		color: #64748b;
		max-width: 600px;
		margin: 0 auto;
	}

	.methods-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 30px;
		margin-bottom: 30px;
	}

	/* Mobile responsive for methods grid */
	@media (max-width: 768px) {
		.methods-grid {
			grid-template-columns: 1fr;
			gap: 20px;
		}
	}

	.methods-grid .emergency-card {
		grid-column: span 3;
		min-height: 120px;
		background: linear-gradient(135deg, #ef4444, #dc2626);
		color: white;
		border-color: transparent;
		position: relative;
		overflow: hidden;
		display: flex;
		align-items: center;
		justify-content: center;
		text-align: center;
		padding: 30px;
	}

	.methods-grid .emergency-card .method-icon {
		margin-bottom: 0;
		margin-right: 20px;
	}

	.methods-grid .emergency-card .method-content {
		display: flex;
		align-items: center;
		gap: 20px;
	}

	.methods-grid .emergency-card .method-details {
		text-align: left;
	}

	.method-card {
		background: white;
		padding: 40px 30px;
		border-radius: 20px;
		border: 2px solid #f1f5f9;
		text-align: center;
		transition: all 0.3s ease;
		position: relative;
		overflow: hidden;
		min-height: 280px;
		display: flex;
		flex-direction: column;
		justify-content: center;
	}

	.method-card::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 4px;
		background: linear-gradient(90deg, #25c1ce, #20b3bc);
		transform: scaleX(0);
		transition: transform 0.3s ease;
	}

	/* Emergency Section - Full Width */
	.emergency-section {
		width: 100%;
	}

	.emergency-card-large {
		background: linear-gradient(135deg, #ef4444, #dc2626);
		color: white;
		border-radius: 20px;
		padding: 40px;
		display: flex;
		align-items: center;
		justify-content: space-between;
		box-shadow: 0 20px 60px rgba(239, 68, 68, 0.2);
		position: relative;
		overflow: hidden;
		transition: all 0.3s ease;
	}

	.emergency-card-large::before {
		content: '⚡';
		position: absolute;
		top: 20px;
		right: 30px;
		font-size: 48px;
		opacity: 0.2;
	}

	.emergency-card-large:hover {
		transform: translateY(-5px);
		box-shadow: 0 25px 70px rgba(239, 68, 68, 0.3);
	}

	.emergency-content {
		display: flex;
		align-items: center;
		gap: 30px;
		flex: 1;
	}

	.emergency-icon {
		width: 80px;
		height: 80px;
		background: rgba(255, 255, 255, 0.2);
		border-radius: 20px;
		display: flex;
		align-items: center;
		justify-content: center;
		flex-shrink: 0;
	}

	.emergency-details h3 {
		font-size: 28px;
		font-weight: 800;
		margin-bottom: 8px;
	}

	.emergency-details p {
		font-size: 18px;
		opacity: 0.9;
		margin-bottom: 16px;
		max-width: 500px;
	}

	.emergency-phone {
		font-size: 24px;
		font-weight: 700;
		color: white;
		text-decoration: none;
		transition: opacity 0.3s ease;
	}

	.emergency-phone:hover {
		opacity: 0.8;
	}

	.emergency-action {
		flex-shrink: 0;
		display: flex;
		justify-content: center;
	}

	.btn-emergency-large {
		display: inline-flex;
		align-items: center;
		gap: 16px;
		padding: 20px 40px;
		background: rgba(255, 255, 255, 0.2);
		color: white;
		text-decoration: none;
		border-radius: 16px;
		font-weight: 700;
		font-size: 18px;
		border: 2px solid rgba(255, 255, 255, 0.3);
		transition: all 0.3s ease;
		backdrop-filter: blur(10px);
	}

	.btn-emergency-large:hover {
		background: rgba(255, 255, 255, 0.3);
		transform: translateY(-2px);
		box-shadow: 0 15px 40px rgba(255, 255, 255, 0.2);
	}

	@media (max-width: 768px) {
		.methods-grid {
			grid-template-columns: 1fr;
		}

		.method-card {
			min-height: 240px;
		}

		.emergency-card-large {
			flex-direction: column;
			text-align: center;
			padding: 30px 20px;
			gap: 30px;
		}

		.emergency-content {
			flex-direction: column;
			gap: 20px;
		}

		.emergency-details h3 {
			font-size: 24px;
		}

		.emergency-details p {
			font-size: 16px;
		}

		.emergency-phone {
			font-size: 20px;
		}

		.btn-emergency-large {
			padding: 16px 32px;
			font-size: 16px;
			width: 100%;
			justify-content: center;
		}

		.emergency-icon {
			width: 60px;
			height: 60px;
		}
	}

	.method-card:hover {
		transform: translateY(-5px);
		box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
		border-color: rgba(37, 193, 206, 0.2);
	}

	.method-card:hover::before {
		transform: scaleX(1);
	}

	.method-icon {
		width: 80px;
		height: 80px;
		background: rgba(37, 193, 206, 0.1);
		border-radius: 20px;
		display: flex;
		align-items: center;
		justify-content: center;
		margin: 0 auto 20px;
	}

	.method-icon.emergency {
		background: linear-gradient(135deg, #ef4444, #dc2626);
	}

	.method-card h3 {
		font-size: 20px;
		font-weight: 700;
		color: #2c3e50;
		margin-bottom: 8px;
	}

	.method-contact {
		margin: 12px 0;
	}

	.method-contact a {
		font-size: 18px;
		font-weight: 600;
		color: #25c1ce;
		text-decoration: none;
	}

	.method-contact a:hover {
		color: #20b3bc;
	}

	.method-time {
		font-size: 14px;
		color: #64748b;
		margin-bottom: 24px;
		display: block;
	}

	.method-action {
		display: flex;
		justify-content: center;
	}

	.btn-outline {
		display: inline-flex;
		padding: 16px 32px;
		background: transparent;
		color: #25c1ce;
		text-decoration: none;
		border-radius: 12px;
		font-weight: 700;
		font-size: 16px;
		border: 2px solid #25c1ce;
		transition: all 0.3s ease;
		min-height: 52px;
		align-items: center;
		justify-content: center;
	}

	.btn-outline:hover {
		background: #25c1ce;
		color: white;
		transform: translateY(-2px);
		box-shadow: 0 10px 30px rgba(37, 193, 206, 0.3);
	}

	.emergency-card {
		background: linear-gradient(135deg, #ef4444, #dc2626);
		color: white;
		border-color: transparent;
	}

	.emergency-card h3,
	.emergency-card .method-contact a,
	.emergency-card .method-time {
		color: white;
	}

	.emergency-card .method-contact a:hover {
		color: rgba(255, 255, 255, 0.8);
	}

	.btn-emergency {
		display: inline-block;
		padding: 16px 32px;
		background: rgba(255, 255, 255, 0.2);
		color: white;
		text-decoration: none;
		border-radius: 12px;
		font-weight: 700;
		font-size: 16px;
		border: 2px solid rgba(255, 255, 255, 0.3);
		transition: all 0.3s ease;
		min-height: 52px;
		display: inline-flex;
		align-items: center;
		justify-content: center;
	}

	.btn-emergency:hover {
		background: rgba(255, 255, 255, 0.3);
		transform: translateY(-2px);
		box-shadow: 0 10px 30px rgba(255, 255, 255, 0.1);
	}

	/* Contact Form Section */
	.contact-form-section {
		padding: 100px 20px;
		background: linear-gradient(135deg, #f8fafc 0%, #f1f5f9 100%);
	}

	.form-container {
		max-width: 1200px;
		margin: 0 auto;
	}

	.form-content {
		display: grid;
		grid-template-columns: 1fr 1.5fr;
		gap: 80px;
		align-items: start;
	}

	.form-info h2 {
		font-size: clamp(2rem, 4vw, 2.5rem);
		font-weight: 800;
		color: #2c3e50;
		margin-bottom: 16px;
	}

	.form-info > p {
		font-size: 18px;
		color: #64748b;
		margin-bottom: 40px;
	}

	.form-features {
		margin-bottom: 40px;
	}

	.feature-item {
		display: flex;
		align-items: center;
		gap: 16px;
		margin-bottom: 16px;
	}

	.feature-icon {
		width: 40px;
		height: 40px;
		background: rgba(37, 193, 206, 0.1);
		border-radius: 10px;
		display: flex;
		align-items: center;
		justify-content: center;
		flex-shrink: 0;
	}

	.feature-item span {
		font-weight: 600;
		color: #2c3e50;
	}

	.info-card {
		background: white;
		padding: 30px;
		border-radius: 16px;
		border: 1px solid rgba(37, 193, 206, 0.1);
	}

	.info-card h3 {
		font-size: 18px;
		font-weight: 700;
		color: #2c3e50;
		margin-bottom: 8px;
	}

	.info-card p {
		color: #64748b;
		margin-bottom: 20px;
	}

	.emergency-btn {
		display: flex;
		align-items: center;
		gap: 12px;
		padding: 16px 24px;
		background: linear-gradient(135deg, #ef4444, #dc2626);
		color: white;
		text-decoration: none;
		border-radius: 12px;
		font-weight: 600;
		transition: all 0.3s ease;
	}

	.emergency-btn:hover {
		transform: translateY(-2px);
		box-shadow: 0 10px 30px rgba(239, 68, 68, 0.3);
	}

	/* Form Styles */
	.form-wrapper {
		background: white;
		border-radius: 20px;
		padding: 50px;
		box-shadow: 0 20px 60px rgba(0, 0, 0, 0.08);
		border: 1px solid rgba(37, 193, 206, 0.1);
	}

	.contact-form {
		display: flex;
		flex-direction: column;
		gap: 24px;
	}

	.form-row {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 24px;
	}

	.form-group {
		display: flex;
		flex-direction: column;
		position: relative;
	}

	.form-group label {
		font-size: 14px;
		font-weight: 600;
		color: #2c3e50;
		margin-bottom: 8px;
	}

	.form-group input,
	.form-group select,
	.form-group textarea {
		padding: 14px 18px;
		border: 2px solid #e2e8f0;
		border-radius: 10px;
		font-size: 16px;
		transition: all 0.3s ease;
		background: white;
	}

	.form-group input:focus,
	.form-group select:focus,
	.form-group textarea:focus {
		outline: none;
		border-color: #25c1ce;
		box-shadow: 0 0 0 3px rgba(37, 193, 206, 0.1);
	}

	.form-group input.error,
	.form-group select.error,
	.form-group textarea.error {
		border-color: #ef4444;
	}

	.form-error {
		font-size: 12px;
		color: #ef4444;
		margin-top: 4px;
		min-height: 16px;
	}

	.form-checkbox {
		display: flex;
		align-items: flex-start;
		gap: 12px;
	}

	.form-checkbox input[type="checkbox"] {
		width: 20px;
		height: 20px;
		margin: 0;
		accent-color: #25c1ce;
	}

	.form-checkbox label {
		font-size: 14px;
		color: #64748b;
		line-height: 1.5;
		margin: 0;
	}

	.form-checkbox a {
		color: #25c1ce;
		text-decoration: none;
		font-weight: 600;
	}

	.form-checkbox a:hover {
		text-decoration: underline;
	}

	.form-actions {
		display: flex;
		justify-content: flex-end;
		margin-top: 16px;
	}

	.submit-btn {
		display: inline-flex;
		align-items: center;
		gap: 12px;
		padding: 16px 32px;
		background: linear-gradient(135deg, #25c1ce 0%, #20b3bc 100%);
		color: white;
		border: none;
		border-radius: 12px;
		font-weight: 700;
		font-size: 16px;
		cursor: pointer;
		transition: all 0.3s ease;
		box-shadow: 0 10px 30px rgba(37, 193, 206, 0.3);
		position: relative;
		overflow: hidden;
	}

	.submit-btn:hover {
		transform: translateY(-2px);
		box-shadow: 0 15px 40px rgba(37, 193, 206, 0.4);
	}

	.submit-btn:disabled {
		opacity: 0.7;
		cursor: not-allowed;
		transform: none;
	}

	.submit-btn::before {
		content: '';
		position: absolute;
		top: 0;
		left: -100%;
		width: 100%;
		height: 100%;
		background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
		transition: left 0.5s ease;
	}

	.submit-btn:hover::before {
		left: 100%;
	}

	.form-message {
		padding: 16px;
		border-radius: 10px;
		font-size: 14px;
		font-weight: 600;
		text-align: center;
		opacity: 0;
		transform: translateY(-10px);
		transition: all 0.3s ease;
	}

	.form-message.success {
		background: rgba(34, 197, 94, 0.1);
		color: #16a34a;
		border: 1px solid rgba(34, 197, 94, 0.2);
		opacity: 1;
		transform: translateY(0);
	}

	.form-message.error {
		background: rgba(239, 68, 68, 0.1);
		color: #dc2626;
		border: 1px solid rgba(239, 68, 68, 0.2);
		opacity: 1;
		transform: translateY(0);
	}

	/* Map Section */
	.map-section {
		padding: 100px 20px;
		background: white;
	}

	.map-container {
		max-width: 1200px;
		margin: 0 auto;
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 80px;
		align-items: center;
	}

	.map-info h2 {
		font-size: clamp(2rem, 4vw, 2.5rem);
		font-weight: 800;
		color: #2c3e50;
		margin-bottom: 16px;
	}

	.map-info > p {
		font-size: 18px;
		color: #64748b;
		margin-bottom: 40px;
	}

	.address-card {
		background: white;
		padding: 30px;
		border-radius: 16px;
		border: 2px solid #f1f5f9;
		margin-bottom: 40px;
	}

	.address-header {
		display: flex;
		align-items: center;
		gap: 20px;
		margin-bottom: 20px;
	}

	.address-icon {
		width: 60px;
		height: 60px;
		background: rgba(37, 193, 206, 0.1);
		border-radius: 12px;
		display: flex;
		align-items: center;
		justify-content: center;
		flex-shrink: 0;
	}

	.address-header h3 {
		font-size: 20px;
		font-weight: 700;
		color: #2c3e50;
	}

	.address .address {
		font-size: 16px;
		color: #2c3e50;
		margin-bottom: 16px;
		line-height: 1.6;
	}

	.directions-link {
		color: #25c1ce;
		text-decoration: none;
		font-weight: 600;
		transition: color 0.3s ease;
	}

	.directions-link:hover {
		color: #20b3bc;
	}

	.office-hours h4 {
		font-size: 18px;
		font-weight: 700;
		color: #2c3e50;
		margin-bottom: 20px;
	}

	.hours-list {
		display: flex;
		flex-direction: column;
		gap: 12px;
	}

	.hours-item {
		display: flex;
		justify-content: space-between;
		padding: 12px 16px;
		background: #f8fafc;
		border-radius: 8px;
		font-size: 14px;
	}

	.hours-item span:first-child {
		color: #64748b;
	}

	.hours-item span:last-child {
		font-weight: 600;
		color: #2c3e50;
	}

	.map-container-interactive {
		position: relative;
		height: 400px;
		border-radius: 20px;
		overflow: hidden;
		box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
	}

	.map-container-interactive iframe {
		width: 100%;
		height: 100%;
		border: none;
	}

	.map-overlay {
		position: absolute;
		bottom: 30px;
		left: 30px;
		background: white;
		padding: 25px;
		border-radius: 16px;
		box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
		max-width: 280px;
		z-index: 10;
	}

	.overlay-content {
		text-align: left;
	}

	.overlay-content .marker-icon {
		width: 50px;
		height: 50px;
		background: rgba(37, 193, 206, 0.1);
		border-radius: 12px;
		display: flex;
		align-items: center;
		justify-content: center;
		margin-bottom: 16px;
		animation: bounce 2s ease-in-out infinite;
	}

	.overlay-content h3 {
		font-size: 18px;
		font-weight: 700;
		color: #2c3e50;
		margin-bottom: 8px;
	}

	.overlay-content p {
		font-size: 14px;
		color: #64748b;
		margin-bottom: 16px;
		line-height: 1.4;
	}

	.directions-btn {
		display: inline-flex;
		align-items: center;
		gap: 8px;
		padding: 10px 16px;
		background: linear-gradient(135deg, #25c1ce 0%, #20b3bc 100%);
		color: white;
		text-decoration: none;
		border-radius: 8px;
		font-weight: 600;
		font-size: 14px;
		transition: all 0.3s ease;
	}

	.directions-btn:hover {
		transform: translateY(-2px);
		box-shadow: 0 5px 20px rgba(37, 193, 206, 0.3);
	}

	/* Animations */
	@keyframes float {
		0%, 100% { transform: translateY(0); }
		50% { transform: translateY(-20px); }
	}

	@keyframes bounce {
		0%, 100% { transform: translateY(0); }
		50% { transform: translateY(-10px); }
	}

	/* Scroll animations */
	.fade-in {
		opacity: 0;
		transform: translateY(30px);
		transition: all 0.6s ease-out;
	}

	.fade-in.visible {
		opacity: 1;
		transform: translateY(0);
	}

	.slide-in-left {
		opacity: 0;
		transform: translateX(-50px);
		transition: all 0.8s ease-out;
	}

	.slide-in-left.visible {
		opacity: 1;
		transform: translateX(0);
	}

	.slide-in-right {
		opacity: 0;
		transform: translateX(50px);
		transition: all 0.8s ease-out;
	}

	.slide-in-right.visible {
		opacity: 1;
		transform: translateX(0);
	}

	.stagger-item {
		opacity: 0;
		transform: translateY(30px);
		transition: all 0.6s ease-out;
	}

	.stagger-item.visible {
		opacity: 1;
		transform: translateY(0);
	}

	.stagger-item:nth-child(1) { transition-delay: 0.1s; }
	.stagger-item:nth-child(2) { transition-delay: 0.2s; }
	.stagger-item:nth-child(3) { transition-delay: 0.3s; }
	.stagger-item:nth-child(4) { transition-delay: 0.4s; }

	/* Responsive Design */
	@media (max-width: 1024px) {
		.hero-content {
			gap: 40px;
		}

		.form-content {
			gap: 60px;
		}

		.map-container {
			gap: 60px;
		}

		.form-wrapper {
			padding: 40px 30px;
		}

		.methods-grid {
			gap: 25px;
		}
	}

	@media (max-width: 768px) {
		/* Hero Section */
		.contact-hero {
			padding: 60px 20px;
		}

		.hero-content {
			grid-template-columns: 1fr;
			gap: 40px;
			text-align: center;
		}

		.hero-text {
			padding-right: 0;
		}

		.hero-cta {
			justify-content: center;
			flex-direction: column;
			align-items: center;
			gap: 16px;
		}

		.cta-primary,
		.cta-secondary {
			width: 100%;
			max-width: 280px;
			justify-content: center;
		}

		.hero-visual {
			height: 250px;
		}

		/* Contact Methods */
		.contact-methods {
			padding: 80px 20px;
		}

		.methods-grid {
			grid-template-columns: 1fr;
		}

		.emergency-card {
			grid-column: span 1;
		}

		.method-card {
			padding: 30px 20px;
		}

		.method-icon {
			width: 60px;
			height: 60px;
		}

		/* Contact Form */
		.contact-form-section {
			padding: 80px 20px;
		}

		.form-content {
			grid-template-columns: 1fr;
			gap: 50px;
		}

		.form-wrapper {
			padding: 30px 20px;
			max-width: 600px;
			margin: 0 auto;
		}

		.form-group label {
			text-align: left;
		}

		.form-checkbox label {
			text-align: left;
		}

		.form-features {
			margin-bottom: 40px;
			display: flex;
			flex-direction: column;
			align-items: center;
		}

		.feature-item {
			display: flex;
			align-items: center;
			gap: 12px;
			margin-bottom: 12px;
			justify-content: flex-start;
			width: 100%;
			max-width: 320px;
		}

		.feature-icon {
			width: 32px;
			height: 32px;
			background: rgba(37, 193, 206, 0.1);
			border-radius: 8px;
			display: flex;
			align-items: center;
			justify-content: center;
			flex-shrink: 0;
		}

		.feature-item span {
			text-align: left;
			flex: 1;
			line-height: 1.4;
		}

		.emergency-btn {
			display: flex;
			align-items: center;
			gap: 12px;
			padding: 16px 24px;
			background: linear-gradient(135deg, #ef4444, #dc2626);
			color: white;
			text-decoration: none;
			border-radius: 12px;
			font-weight: 600;
			transition: all 0.3s ease;
			white-space: nowrap;
			font-size: 14px;
		}

		.form-row {
			grid-template-columns: 1fr;
		}

		/* Map Section */
		.map-section {
			padding: 80px 20px;
		}

		.map-container {
			grid-template-columns: 1fr;
			gap: 50px;
		}

		.map-container-interactive {
			height: 300px;
		}

		.map-overlay {
			bottom: 20px;
			left: 20px;
			right: 20px;
			max-width: none;
			padding: 20px;
		}

		.address-card {
			padding: 25px 20px;
		}

		/* Typography */
		.hero-title,
		.methods-title,
		.form-info h2,
		.map-info h2 {
			font-size: 2rem;
		}

		.section-title {
			font-size: clamp(1.5rem, 6vw, 2rem);
		}

		/* Form elements */
		.form-group input,
		.form-group select,
		.form-group textarea {
			padding: 12px 16px;
		}

		.submit-btn {
			padding: 14px 28px;
			font-size: 15px;
		}

		/* CTA buttons */
		.cta-primary,
		.cta-secondary {
			padding: 14px 28px;
			font-size: 15px;
		}
	}

	@media (max-width: 480px) {
		/* Small mobile adjustments */
		.contact-hero {
			padding: 30px 10px 20px;
		}

		.hero-content {
			max-width: 100%;
			width: 100%;
		}

		.hero-text {
			padding-right: 0;
		}

		.hero-cta {
			flex-direction: column;
			gap: 12px;
			width: 100%;
			max-width: 280px;
			margin: 0 auto;
		}

		.cta-primary,
		.cta-secondary {
			width: 100%;
			max-width: 100%;
			padding: 14px 20px;
			font-size: 0.95rem;
			min-height: 48px;
			box-sizing: border-box;
			white-space: nowrap;
			overflow: hidden;
			text-overflow: ellipsis;
		}

		.hero-visual {
			height: 200px;
		}

		/* Floating Cards adjustments for mobile */
		.card {
			padding: 16px;
			min-width: 100px;
			transform: scale(0.85);
		}

		.card-icon {
			width: 40px;
			height: 40px;
		}

		.card span {
			font-size: 12px;
		}

		.card-1 {
			top: 10px;
			right: 10px;
			z-index: 3;
		}

		.card-2 {
			bottom: 15px;
			left: 10px;
			z-index: 2;
		}

		.card-3 {
			top: 50%;
			left: 50%;
			transform: translate(-50%, -50%) scale(0.85);
			z-index: 1;
		}

		.contact-methods,
		.contact-form-section,
		.map-section {
			padding: 40px 15px;
		}

		.methods-container,
		.form-container,
		.map-container {
			max-width: 100%;
		}

		.method-card {
			padding: 20px 15px;
			text-align: center;
		}

		.method-card h3 {
			font-size: 1.1rem;
			margin-bottom: 8px;
		}

		.method-contact {
			font-size: 0.95rem;
			margin-bottom: 5px;
		}

		.method-contact a {
			white-space: nowrap;
			font-weight: 600;
		}

		.method-time {
			font-size: 0.85rem;
			margin-bottom: 15px;
		}

		.method-icon {
			width: 50px;
			height: 50px;
			margin: 0 auto 15px;
		}

		.btn-outline {
			padding: 10px 16px;
			font-size: 0.9rem;
			min-height: 40px;
			box-sizing: border-box;
			width: 100%;
			max-width: 200px;
			margin: 0 auto;
			display: flex;
			align-items: center;
			justify-content: center;
		}

		.form-wrapper {
			padding: 20px 12px;
		}

		.form-group input,
		.form-group select,
		.form-group textarea {
			padding: 12px 14px;
			font-size: 14px;
		}

		.submit-btn {
			width: 100%;
			padding: 14px 20px;
			font-size: 0.95rem;
			min-height: 48px;
			justify-content: center;
		}

		.address-card {
			padding: 16px 12px;
		}

		.hero-title,
		.methods-title,
		.form-info h2,
		.map-info h2 {
			font-size: 1.6rem;
			line-height: 1.2;
		}

		.hero-description,
		.methods-description,
		.form-info > p,
		.map-info > p {
			font-size: 0.9rem;
		}

		.emergency-card-large {
			padding: 20px 15px;
			text-align: center;
		}

		.emergency-content {
			text-align: center;
		}

		.emergency-details {
			text-align: center;
		}

		.emergency-phone {
			font-size: 1.1rem;
			white-space: nowrap;
		}

		.emergency-action {
			text-align: center;
		}

		.btn-emergency-large {
			display: inline-flex;
			margin: 0 auto;
		}

		.map-overlay {
			bottom: 15px;
			left: 15px;
			right: 15px;
			padding: 15px;
		}

		.directions-btn {
			padding: 8px 12px;
			font-size: 0.85rem;
		}
	}
</style>

<script>
	document.addEventListener('DOMContentLoaded', () => {
		// Intersection Observer for animations
		const observerOptions = {
			threshold: 0.1,
			rootMargin: '0px 0px -50px 0px'
		};

		const observer = new IntersectionObserver((entries) => {
			entries.forEach(entry => {
				if (entry.isIntersecting) {
					entry.target.classList.add('visible');
				}
			});
		}, observerOptions);

		document.querySelectorAll('.fade-in, .slide-in-left, .slide-in-right, .stagger-item').forEach(el => {
			observer.observe(el);
		});

		// Form handling
		const form = document.getElementById('contactForm');
		const submitBtn = form.querySelector('.submit-btn');
		const formMessage = document.getElementById('formMessage');

		// Form validation
		const validateForm = (formData) => {
			const errors = {};

			if (!formData.firstName.trim()) {
				errors.firstName = 'Voornaam is verplicht';
			}

			if (!formData.lastName.trim()) {
				errors.lastName = 'Achternaam is verplicht';
			}

			const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
			if (!formData.email.trim()) {
				errors.email = 'E-mailadres is verplicht';
			} else if (!emailRegex.test(formData.email)) {
				errors.email = 'Voer een geldig e-mailadres in';
			}

			if (formData.phone && !/^[\d\s\-\+\(\)]+$/.test(formData.phone)) {
				errors.phone = 'Voer een geldig telefoonnummer in';
			}

			if (!formData.subject) {
				errors.subject = 'Selecteer een onderwerp';
			}

			if (!formData.message.trim()) {
				errors.message = 'Bericht is verplicht';
			} else if (formData.message.trim().length < 10) {
				errors.message = 'Bericht moet minimaal 10 tekens bevatten';
			}

			if (!formData.privacy) {
				errors.privacy = 'U moet akkoord gaan met de privacyvoorwaarden';
			}

			return errors;
		};

		// Display form errors
		const displayErrors = (errors) => {
			form.querySelectorAll('.form-error').forEach(error => {
				error.textContent = '';
			});
			form.querySelectorAll('input, select, textarea').forEach(field => {
				field.classList.remove('error');
			});

			Object.keys(errors).forEach(fieldName => {
				const field = form.querySelector(`[name="${fieldName}"]`);
				if (field) {
					field.classList.add('error');
					const errorElement = field.parentElement.querySelector('.form-error');
					if (errorElement) {
						errorElement.textContent = errors[fieldName];
					}
				}
			});
		};

		// Handle form submission
		form.addEventListener('submit', async (e) => {
			e.preventDefault();

			const formData = new FormData(form);
			const data = Object.fromEntries(formData.entries());

			const errors = validateForm(data);
			if (Object.keys(errors).length > 0) {
				displayErrors(errors);
				return;
			}

			displayErrors({});

			submitBtn.disabled = true;
			submitBtn.innerHTML = `
				<span>Versturen...</span>
				<svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
					<circle cx="12" cy="12" r="10" stroke="currentColor" stroke-width="2" stroke-dasharray="60" stroke-dashoffset="60">
						<animate attributeName="stroke-dashoffset" from="60" to="0" dur="1s" repeatCount="indefinite"/>
					</circle>
				</svg>
			`;

			try {
				await new Promise(resolve => setTimeout(resolve, 2000));

				formMessage.className = 'form-message success';
				formMessage.textContent = 'Uw bericht is succesvol verzonden! Wij nemen binnen 24 uur contact met u op.';

				form.reset();

				setTimeout(() => {
					formMessage.className = 'form-message';
					formMessage.textContent = '';
				}, 5000);

			} catch (error) {
				formMessage.className = 'form-message error';
				formMessage.textContent = 'Er is een fout opgetreden. Probeer het opnieuw of neem telefonisch contact op.';

				setTimeout(() => {
					formMessage.className = 'form-message';
					formMessage.textContent = '';
				}, 5000);
			} finally {
				submitBtn.disabled = false;
				submitBtn.innerHTML = `
					<span>Verstuur Bericht</span>
					<svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
						<path d="M5 12H19M19 12L12 5M19 12L12 19" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
					</svg>
				`;
			}
		});

		// Clear errors on input
		form.querySelectorAll('input, select, textarea').forEach(field => {
			field.addEventListener('input', () => {
				field.classList.remove('error');
				const errorElement = field.parentElement.querySelector('.form-error');
				if (errorElement) {
					errorElement.textContent = '';
				}
			});
		});

		// Smooth scrolling for anchor links
		document.querySelectorAll('a[href^="#"]').forEach(anchor => {
			anchor.addEventListener('click', function (e) {
				e.preventDefault();
				const target = document.querySelector(this.getAttribute('href'));
				if (target) {
					target.scrollIntoView({
						behavior: 'smooth',
						block: 'start'
					});
				}
			});
		});
	});
</script>